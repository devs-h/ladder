{
  "name": "ladder-workspace",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "demo"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "scripts": {
    "ladder:dev": "bun run -F ladder dev",
    "demo:dev": "bun run -F demo dev",
    "ladder:build": "bun run -F ladder build",
    "build": "bun run ladder:build"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
